import { createElement } from 'react';
export default function createComponentWithOrderedProps(component, ...names) {
    return (props, ref) => {
        const orderedProps = {};
        for (const name of Object.keys(props)) {
            if (!names.includes(name)) {
                orderedProps[name] = props[name];
            }
        }
        for (const name of names) {
            orderedProps[name] = props[name];
        }
        orderedProps.ref = ref;
        return createElement(component, orderedProps);
    };
}
//# sourceMappingURL=createComponentWIthOrderedProps.js.map